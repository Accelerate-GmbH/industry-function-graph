# SPDX-License-Identifier: MIT

# Publishes generated/ to GitHub Pages, so that the matrix, the graph and the
# ontology are fetchable under the namespace the concepts are minted in,
# https://didas-swiss.github.io/industry-function-graph/.
#
# Requires: Settings > Pages > Source = "GitHub Actions" (not "Deploy from a branch").
# The configure-pages step asks to create the site itself (enablement: true), but
# this organization does not let the Actions token do that - it fails with
# "Create Pages site failed / Resource not accessible by integration" - so the
# setting has to be flipped once by hand. The flag is kept because it is correct
# where the token is allowed, and harmless once the site exists.

name: Deploy Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-pages:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Pages
        uses: actions/configure-pages@v4
        with:
          # creates the site on first run where org policy allows it
          enablement: true

      - name: Assemble static output
        run: |
          mkdir -p dist
          cp generated/index.html generated/ifm-graph.ttl generated/ifm-graph.jsonld dist/
          # served both at the ontology IRI itself and under a .ttl extension
          cp ontology/ifm.ttl dist/ontology
          cp ontology/ifm.ttl dist/ontology.ttl

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy-pages:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build-pages
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4